# Builds the ingestion image (Dockerfile is at ingestion/Dockerfile, context = repo root).
steps:
  - name: gcr.io/cloud-builders/docker
    args: ["build", "-f", "ingestion/Dockerfile", "-t", "${_IMAGE}", "."]
images: ["${_IMAGE}"]
options:
  logging: CLOUD_LOGGING_ONLY
